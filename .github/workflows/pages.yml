# .github/workflows/pages.yml
name: Quarto → Pages

on:
  push:
    branches: [ "main" ]   # change if your default branch differs

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4


      - uses: astral-sh/setup-uv@v6
        with:
          version: "0.8.18"

      # Quarto CLI (no manual apt/wget needed)
      - uses: quarto-dev/quarto-actions/setup@v2

      # Build your site (adjust if your Makefile target differs)
      - name: Build site
        shell: bash
        run: |
          uv sync
          source .venv/bin/activate
          make website
          
      # Upload the built site folder
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site-combined

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4

  commit-notebooks:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: gather notebooks
        shell: bash
        run: |
          mkdir -p built_notebooks/
          mkdir -p built_notebooks/student/
          mkdir -p built_notebooks/instructor/
          cp _site_combined/student/lessons/*.ipynb built_notebooks/student/.
          cp _site_combined/instructor/lessons/*.ipynb built_notebooks/instructor/.

      - uses: EndBug/add-and-commit@v9 # You can change this to use a specific version.
        with:
          # The arguments for the `git add` command (see the paragraph below for more info)
          # Default: '.'
          add: 'built_notebooks/*'
          default_author: github_actor
          message: 'Update notebooks'
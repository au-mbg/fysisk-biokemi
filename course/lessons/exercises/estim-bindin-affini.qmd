## Estimation of binding affinity 

#### (a) Train estimation skills

Train your estimation skills using the widget below. 

```{python}
#| eval: false
#| 
from fysisk_biokemi.widgets.utils.colab import enable_custom_widget_colab
from fysisk_biokemi.widgets import estimate_kd

enable_custom_widget_colab()
estimate_kd()
```

#### (b) Compare to quadratic

The widget below shows the curves for $\theta$ using both the simple expression assuming 
that $L = L_{tot}$ and the quadratic binding expression. Vary $K_D$ and $P_{total}$ to work 
out a rule of thumb for when the two equations give a similar curve. 

```{python}
#| eval: false
from fysisk_biokemi.widgets.utils.colab import enable_custom_widget_colab
from fysisk_biokemi.widgets import visualize_simple_vs_quadratic

enable_custom_widget_colab()
visualize_simple_vs_quadratic()
```

```{python}
#| solution: true
answer = "They are equal when P_tot << K_D"
```
